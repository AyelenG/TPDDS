package model;

import org.json.simple.JSONObject;
import org.uqbar.commons.utils.Observable;

public class ManipuladorDeJson {
/*	public static Empresa crearEmpresa(Object jsonEmpresa) {
		Empresa empresa = new Empresa();
		empresa.setNombreEmpresa(((JSONObject) jsonEmpresa).get("nombreEmpresa").toString());
		return empresa;
	}
	
	public static Periodo crearPeriodo(Object jsonPeriodo) {
		Periodo periodo = new Periodo();
		periodo.setNombrePeriodo(((JSONObject) jsonPeriodo).get("nombrePeriodo").toString());
		return periodo;
	}
	
	public static Cuenta crearCuenta(Object jsonCuenta) {
		Cuenta cuenta = new Cuenta();
		cuenta.setNombreCuenta(((JSONObject) jsonCuenta).get("nombreCuenta").toString());
		cuenta.setValor(((Number) ((JSONObject) jsonCuenta).get("valor")).longValue());
		return cuenta;
	}*/
}
